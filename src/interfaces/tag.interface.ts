export interface TagInterface {
    id: number,
    label: string,
    slug: string,
    forceShow: boolean,
    publishedAt: string,
    updatedBy: number,
    createdAt: string,
    updatedAt: string,
    forceHide: boolean,
    link: string
}